name: Deploy to Fargate

on:
  push:
    branches:
      - main  

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1

      - name: Log in to Amazon ECR
        uses: aws-actions/amazon-ecr-login@v1

      - name: Build and Push Docker image
        run: |
          docker buildx build --platform linux/amd64,linux/arm64 -t 319700097359.dkr.ecr.us-east-2.amazonaws.com/claude-app:latest .
          docker push 319700097359.dkr.ecr.us-east-2.amazonaws.com/claude-app:latest

      - name: Deploy to AWS Fargate
        run: |
          aws ecs update-service \
            --cluster claude-cluster \
            --service claude-task-service-hcgdhzyx \
            --force-new-deployment
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_REGION: us-east-2

